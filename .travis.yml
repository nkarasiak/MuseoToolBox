language: python
matrix:
#    allow_failures:
#        - os: osx
    include:
#        - os: osx
#          language: generic
        - os: linux
          sudo: required
          python: 3.4
        - os: linux
          sudo: required
          python: 3.5
        - os: linux
          sudo: required
          python: 3.6

install:
  - pip install -r requirements.txt
  - pip install .

script:
  - pytest
